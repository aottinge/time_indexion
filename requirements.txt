# Noyau — installation minimale (~2 Go avec PyTorch CPU)
chromadb>=1.5.0
pillow>=10.0.0
pymupdf>=1.24.0
sentence-transformers>=3.0.0

# PyTorch CPU uniquement (évite le téléchargement CUDA ~5 Go)
# Si déjà installé, pip ignore cette ligne.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
